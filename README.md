# JVET_Pipeline
A Python pipeline for batch downloading and analyzing JVET standard proposals. It automatically fetches documents from the JVET website, converts them to text, merges them, and leverages LLM (DeepSeek) to generate structured technical analysis reports.
